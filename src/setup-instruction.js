/**
 * @module root_file
 *
 * @Description
 *
 * MZ-CORE is a production core for high traffic and complex systems/subsystems well organized and well documented easy to set up and utilize
 * web-framework :fasify
 * running mode : clusters ( stick session ) / web socket support
 *
 * auto route registration
 * auto models registration
 * centralized repository
 * synchronized web response
 * logging full control over primary and worker well handled logging on web response and system alerts
 * PG-DB ORM
 * cache layer ( redis ) ready plugin's
 * _sys (global access over all the core )
 *
 *
 *
 *
 */